@using Regate
@{
    ViewData["Title"] = "Regate";
}

<h1>Regate</h1>


<form method="get">

    <div>@Html.Raw(RegateText.Build("username"))</div>
    <div>@Html.Raw(RegateText.Build("username", "mojtaba"))</div>
    <div>@Html.Raw(RegateText.Build("username", true))</div>
    <div>@Html.Raw(RegateText.Build("username", "mojtaba", true))</div>
    <div>
        @Html.Raw(RegateText.Build("username", new RegateText.Options()
             {
                 Placeholder = "Username"
             }))
    </div>
    
    

    <button type="submit">SUBMIT</button>
</form>
